import type { Metadata } from "next";
import "./globals.css";
import Navbar from "@/components/Navbar";
import Footer from "@/components/Footer";

export const metadata: Metadata = {
  title: "Harmonizer | Battery Energy Storage Systems",
  description: "Harmonizer India – Advanced BESS solutions for industrial, commercial and renewable energy applications. ISO 9001 | ISO 14001 | ISO 45001 certified.",
};

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="en">
      <body>
        <Navbar />
        <main>{children}</main>
        <Footer />
      </body>
    </html>
  );
}
